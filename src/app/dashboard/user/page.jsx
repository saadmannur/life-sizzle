

import { getLessonData } from '@/api/getData';
import React from 'react';

const UserDashboard = async () => {


    // const lessons = await getLessonData() // inside mongodb check
    // console.log(lessons);


    return (
        <div>
            <h2>this is user dashboard</h2>
        </div>
    );
};

export default UserDashboard;